// Placeholder — proves the responsive shell's breakpoint switch works.
// Replace with the real design-matched sidebar (see nav-components-handoff.html).
const DesktopSidebar = () => {
  return (
    <nav className="flex w-56 shrink-0 items-center justify-center border-r border-border bg-bg-subtle text-center text-sm font-bold text-muted">
      DesktopSidebar
      <br />
      (placeholder)
    </nav>
  );
};

export default DesktopSidebar;
